FROM golang:1.26-alpine

RUN apk add --no-cache git

WORKDIR /app

COPY go.mod go.sum ./
RUN go mod download

COPY . .

# Build the test binary so that the container can be executed against a running app
CMD ["go", "test", "-v", "-count=1", "-tags=integration", "./test/integration/..."]
